const sideValue = parseInt( prompt ('Enter side value of rectangle: '))

// calculate area of rectangle

const roundValue = sideValue + sideValue + sideValue
const areaValue = sideValue * sideValue

console.log (
    `The area of the rectangle is ${areaValue}`
)

console.log (
    `The round of the rectangle is ${roundValue}`
)